hello!

instructions:

1. uv venv
2. uv pip install -r requirements.txt
3. python dummy_generator.py
4. streamlit run app.py
5. Rscript validation.R

i already have the raw data, as well as the cleaned one. just waiting for instructions. :)